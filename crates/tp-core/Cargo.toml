[package]
name = "tp-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
# Timestamps on log lines. The fleet's logger omits them because a collector
# stamps at ingest; teleport ships to laptops where launchd writes the file and
# nothing stamps anything, so the line has to carry its own.
chrono.workspace = true
# Restoring SIGPIPE's default disposition — see logging.rs / lib.rs.
libc.workspace = true

[lints]
workspace = true
